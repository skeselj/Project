awk 'BEGIN {
	FS = ","
	count = 1
}

{
	if (NR > 1) {
		found = 0
		for (i = 1; i < count; i++) {
			if (types[i] == $3) {
				found = 1
				break		
			}
		}
		if (found == 0) {
			types[count] = $3
			count = count + 1
		}
	}
}
END {
	for (i = 1; i < count; i++)
		print(types[i]);
	
}' 'processedchicago15.csv' > 'results'